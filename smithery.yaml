name: thumbforge
description: Image resize, thumbnail, format conversion for AI agents
author: AgentPay Labs
url: https://github.com/Rumblingb/thumbforge
source:
  type: git
  url: https://github.com/Rumblingb/thumbforge.git
  branch: master
platforms:
  - linux
  - darwin
  - windows
# Optional: specify the entrypoint if different from server.py
# entrypoint: server.py
# Environment variables (if any)
# env:
#   - name: EXAMPLE_VAR
#     description: An example environment variable
#     required: false
# Optional: dependencies (if not using requirements.txt)
# dependencies:
#   - name: pillow
#     version: ">=10.0.0"